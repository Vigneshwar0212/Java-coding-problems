//Method on logic own concept
//charAt method

public class Main{
    
    public static void main(String[] args){
        String ch = "apple banana carrot";
        for (int i = ch.length() - 1; i>=0; i-- ){
            System.out.print(ch.charAt(i));
        }
        
    }
}

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//charAt method to reverse a string

public class Main{
    
    public static void main(String[] args){
        String str = "abcdefgh";
        String rev = "";
        
        for (int i = str.length()-1; i>=0; i--){    // i = 8-1, i=7 at the 7th index "h" is stored -> then 6th index value "g"......
            rev=rev+str.charAt(i); // rev=null -> rev= null+h, rev =h -> rev = h+g, rev =hg......
        }
        System.out.println("Entered string is: "+str);
        System.out.println("Reverse of the string is: "+rev);
        
    }
}

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
// using a tocharArray() method

public class Main{
    
    public static void main(String[] args){
        String str = "abcdefgh";
        String rev = "";
        char a[] = str.toCharArray();  //convert the string to a character array
        int len = str.length();
        
        for (int i = len -1; i>=0; i--){
            rev = rev+a[i];
        }
        System.out.println("Reversed string is: "+rev);
    }
}

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//using a StringBuffer method

public class Main{
    
    public static void main(String[] args){
        String str = "abcdefgh";
        
        
        StringBuffer sbr = new StringBuffer(str);
        StringBuffer rev = sbr.reverse();
        System.out.println("Reversed string is: "+rev);
    }
}

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//the same methods can also be used with Scanner method as follows:

import java.util.Scanner;

public class Main{
    
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter a String: ");
        String str = sc.nextLine();
        int len = str.length();
        String rev = "";
        for(int i=len-1; i>=0; i--){
            rev = rev+str.charAt(i);
        }
        System.out.println("Reversed string is: "+rev);
    }
}
